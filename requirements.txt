# Install sentence-transformers FIRST — it pins torch/transformers/accelerate versions.
# Installing other packages before it causes silent version conflicts.
sentence-transformers>=2.7.0
chromadb>=0.5.0
spacy>=3.8.0
anthropic>=0.25.0
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-multipart==0.0.6
pandas>=2.0.0
python-dotenv==1.0.1
numpy>=1.26.0
pdfplumber>=0.10.0

# spaCy model will be installed separately